#include <stdio.h>
#include <string.h>

void main(void) {
	char str1[10] = "first";
	// USING strlen is for string lenght
	//Check how many characters there are in a string
	
	printf("The lenght of the string '%s' is %d \n", str1, strlen(str1));
}